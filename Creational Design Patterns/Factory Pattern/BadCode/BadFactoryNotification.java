/**
 * The Product Interface.
 * Even in 'Bad Code', we might use interfaces.
 * The problem isn't the interface itself, but how we instantiate the implementations.
 */
public interface BadFactoryNotification {
    void notifyUser(String message);
}
